1.------------------
## Github
# ■ 자바




2.------------------
# ■ ORACLE
X


# ■ JAVA
# ■ JSP
X




# ■ SPRING
spring005_board




필기................................................

------------------------------
#6. Upload

------------------------------
1. model
    > table : 이미지 경로필드
        SQL> desc sboard1;
        Name                                      Null?    Type
        ----------------------------------------- -------- ----------------------------
        ID                                        NOT NULL NUMBER
        APP_USER_ID                               NOT NULL NUMBER
        BTITLE                                    NOT NULL VARCHAR2(1000)
        BCONTENT                                  NOT NULL CLOB
        BPASS                                     NOT NULL VARCHAR2(255)
        BFILE                                              VARCHAR2(255)
        BHIT                                               NUMBER(10)
        BIP                                       NOT NULL VARCHAR2(255)
        CREATE_AT                                          TIMESTAMP(6)

    dto : bfile
    dao : insert, update

    sboard1-mapper.xml
    <insert id="insert2" parameterType="Sboard1Dto">
  		insert into sboard1 (id, app_user_id, btitle, bcontent, bpass, bfile, bip) 
         values(sboard1_seq.nextval, #{appUserId}, #{btitle}, #{bcontent}, #{bpass}, #{bfile}, #{bip})
	</insert>

 	<update id="update2" parameterType="Sboard1Dto">
 	    update sboard1 
		set BTITLE=#{btitle}, BCONTENT=#{bcontent}, BFILE=#{bfile}
		where id=#{id} and bpass=#{bpass}
	</update>

2. view
    form : encType = "multipart/form-data"      method="post"
           <input type="file" /> <!-- 파일올리고 내릴때 입력받는 창.-->

3. controller
    pom.xml
        <!-- img upload   start -->
        <!-- img upload   start -->
        <!-- https://mvnrepository.com/artifact/commons-fileupload/commons-fileupload -->
        <dependency>
            <groupId>commons-fileupload</groupId>
            <artifactId>commons-fileupload</artifactId>
            <version>1.3.1</version>
        </dependency>
        <!-- img upload end -->
        <!-- img upload end -->    

    servlet-context.xml    
    FileUpload.java

    UploadUtil.java - service
    Controller.java - service

연습문제.......................................

```
1) sboard1
    Q1> 이미지업로드기능추가
    Q2> 수정시 파일을 바꾸지 않으면 기존의 올린값이 나오고, 
            파일을 오리면 값 바꾸게 처리

2) appuser
    Q1. table 에 추가
        ufile  varchar2(255) default 'mamber.png'
    Q2. 멤버가입시(회원가입시) 이미지올리기 가능
    Q3. 수정시 이미지 수정가능


Upgrade1) 회원가입시 (controller-안내파트/service-처리파트★)
          만약에 사용자가 이미지를 안올리면
          7개의 유저이미지가 랜덤하게 올라가게 만들기.

          만약 파일을 올린게 있다면......파일올리기
          아니라면 랜덤하게 이미지경로추출해서 setUfile()
```
------------------------------
#7. Ajax

------------------------------
1. 글 검색
2. 아이디중복검사
3. 유저수정, 삭제

1) model
table, dto, dao#, service)
    select * from emp where ename like %S%;

2) controller 
@RestController

3) view ( ajax )



------------------------------
#8. PAGING

------------------------------
