1.------------------
## Github
### ■ 자바
X




2.------------------
### ■ ORACLE
cmd 사용
pawject4_Exec.sql




### ■ JAVA
### ■ SPRING_BOOT
X



### ■ node + react
Project



### ■ Project
pawject4 



### ■ Linux+Aws+gitAction
cicd




필기................................................
## Ubuntu 24.04 컨테이너 실행 방법
1. **이미지 다운로드**
   ```bash
   docker pull ubuntu:24.04
   ```

2. **컨테이너 실행**
   ```bash 
   docker run -it --name myubuntu ubuntu:24.04 bash
   ```
   → 실행하면 컨테이너 내부의 쉘(`bash`) 
    - it : i(표준입력), t(터미널)
    - --name myubuntu 컨테이너이름
    - ubuntu:24.04 버전
    - bash

3. **컨테이너 내부에서 패키지 업데이트**
   ```bash
   apt update && apt upgrade -y
   ```

4. **컨테이너 종료 후 다시 실행하기**
   - 실행 중인 컨테이너 확인:
     ```bash
     docker ps -a
     ```
   - 컨테이너 재실행:
     ```bash
     docker start -ai <컨테이너_ID>
     docker start -ai myubuntu 
     ```
    - 실행 중인 컨테이너에 접속:
     ```bash
    docker exec -it myubuntu bash
    ```
 

## 2. linux 사용자
1. # root 사용자
2. $ 일반사용자


## 3. 기본명령어 
```bash
# 날짜
date 

# 출력
echo hello
# 령령어 위치 확인
which date

# 명령어 설명서
man
```
```bash
apt update
apt install man-db
unminimize
```

```bash
help echo → 쉘 내장명령어 
man  date → 실행파일

q → 빠져나오기

type echo
type date
```

Q1. hi 출력
Q2. date 사용방법확인 


```
↑ 
↓
date "+Y-%m-%d"
date "+%Y-%m-%d"

```


## 4. 파일
### 파일 및 디렉토리 생성
- touch 파일명 : 빈 파일 생성
- mkdir 디렉토리명 : 새 디렉토리 생성
- mkdir -p 경로/하위디렉토리 : 중첩 디렉토리 생성

### 파일 확인 및 경로 이동
- ls : 현재 디렉토리 목록 보기
- ls -l : 상세 정보 포함 목록
- pwd : 현재 경로 출력
- cd 디렉토리명 : 디렉토리 이동
- cd .. : 상위 디렉토리로 이동

### 삭제 및 복사
- rm 파일명 : 파일 삭제
- rm -r 디렉토리명 : 디렉토리 삭제
- cp 원본 대상 : 파일 복사 (백업)
    cp 파일명(file1.txt) backup.txt
- mv 원본 대상 : 파일 이동 또는 이름 변경
    mv backup.txt final.txt

Q1. testdir 폴더만들기
Q2. 폴더안에  file1.txt 파일만들기 (cd testdir 입력 후 생성) 
Q3. 파일확인 - 디렉토리인지, 폴더인지까지 구분   

### 파일에 적기(★암기★) >: 덮어쓰기, >>: 이어쓰기
- echo "하고 싶은말(fist)" > 파일명(file1.txt)  <!-- >: 덮어쓰기 -->
- cat 파일명(file1.txt)
- echo "second" >> file1.txt <!-- >>: 이어쓰기 -->

Q1. file1.txt에 입력
    apple 
    banana
    cococnut

### 파일에 적기(★암기★) (2) 여러줄 쓰기, 파일편집
### 여러줄
cat > file2.txt 
첫 번째 줄 입력
두 번째 줄 입력 
Ctrl+D # 입력 종료

### vi 에디터
1. sudo vi file1.txt 실행  
2. vi 안에서 Esc 눌러 명령 모드로 전환   
3. i 눌러 입력 모드로 전환 → 새 설정 붙여넣기  
4. Esc → :wq(!) → 저장 후 종료 (강제저장)  

Q. file2.txt vi 에디터 이용해서
one-1
two-2
three-3
편집하기


```
설치 시에는 무조건 apt update 먼저 하고 설치하기!!!
apt update
apt install vim -y( 무조건 Yes )
y
5 (Asia)
68 (Seoul)
```

>> linux에서
```
※참고사항※
한국어 기준
apt update
apt install locales
locale-gen ko_KR.UTF-8
update-locale LANG=ko_KR.UTF-8
```

Q1. 파일만들기   mylinux.txt
Q2. 파일안에 답채우기
-    출력 - echo
-    사용서 - man
-    파일생성 - touch
-    디렉토리만들기 - mkdir
-    목록보기 - ls (-al : 상세보기)
-    상위이동 - cd..
-    파일,폴더삭제 - rm -r
-    file1.txt 을 back.txt으로 파일복사  - cp file1.txt back.txt
-    back.txt를 test.txt로 이름변경  - mv back.txt test.txt
Q3. vi이용해서 맨위에 작성자본인이름 추가 
Q5. mylinux.txt 백업해서 ubuntu에 backup.txt로 
Q6. 상위로 이동 testdir 삭제


## 5. 유저
# 사용자 정보 확인
whoami
id
who 
users
groups

# 사용자 추가 및 삭제 (sudo - 일반권한일때/명령어 입력안될때 사용.)
sudo adduser sally
sudo passwd sally
sudo deluser sally

# 권한 구조 및 변경
ls -l
sudo chown sally:sally hello.txt
chmod 755 hello.txt
umask

```
apt update 
apt install adduser
adduser sally
```
```
d rwx r-x --- 2 hsh  hsh  4096 Feb  3 15:04 .
→ d 디렉토리  소유자(읽기:4/쓰기:2/실행:1)  그룹(읽기:4/쓰기:-/실행:1)    다른사람(읽기:-/쓰기:-/실행:-)

d rwx r-x r-x 1 root root 4096 Feb  3 14:59 ..
→ d 디렉토리  소유자(읽기:4/쓰기:2/실행:1)  그룹(읽기:4/쓰기:-/실행:1)    다른사람(읽기:4/쓰기:-/실행:1)
```

Q1. alpha:유저 만들기 / 비번:1234
Q2. alpha로 로그인 / alpha home 디렉토리 찾아가기
Q3. alpha로 접속해서    /home/hsh 찾아가는거 가능한지 확인 > 못함.

```
chmod  755   /home/hsh
ls -l
```
Q4. alpha로 다시 접속했을대 /home/hsh 접속가능
Q5. /home/hsh 다른사람(읽기:-/쓰기:-/실행:-) 권한주기


## 6. 쉘스크립트 (면접 예상문제)
1. 프로세스 상태확인
```
ps -ef
```
-e : 모든 프로세스
-f : 출력정보 자세히

2. 실시간 모니터링
```
top 
ctrl + c : 빠져나오기
```
3. ip주소 확인
```
ifconfig
```

```
apt update
apt install net-tools
```



4. Hello World 출력 쉘스크립트 작성 (#: 답안)

```
vi hello.sh

#!/bin/bash         ##
echo "Hello World"  ##
```
```
- rw- r-- r-- 1 root   root     31 Feb  3 16:37 hello.sh
```


```
ls -al               ##
chmod +x hello.sh    ##
```
```
- rwx r-x r-x 1 root   root     31 Feb  3 16:37 hello.sh
```


5. 쉘스크립트 실행
```
./hello.sh           ##
```


## Part002. aws
> aws 회원가입
: https://aws.amazon.com/ko/

..............................................................

## Part003. ci/cd






